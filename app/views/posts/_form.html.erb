<%= form_with(model: post, local: true) do |form| %>

<div class="form-group row justify-content-center">
  <div class="col-8">
    <%= form.text_field :title, autofocus: true, placeholder: "Título", required: true, autocomplete: "email", class: "form-control" %>
  </div>
</div>

<div class="form-group row justify-content-center">
  <div class="col-8">
    <ul class="nav nav-tabs customtab" role="tablist">
        <li class="nav-item"> <a class="nav-link active show" data-toggle="tab" href="#texto" role="tab" aria-selected="true"><span class="hidden-sm-up"><i class="ti-home"></i></span> <span class="hidden-xs-down">TEXTO</span></a> </li>
        <li class="nav-item"> <a class="nav-link" data-toggle="tab" href="#html" role="tab" aria-selected="false"><span class="hidden-sm-up"><i class="ti-user"></i></span> <span class="hidden-xs-down">HTML</span></a> </li>
    </ul>

    <div class="tab-content">
    <div class="tab-pane active show p-t-20" id="texto" role="tabpanel">
        <trix-editor input="post_description"></trix-editor>
    </div>
    <div class="tab-pane p-t-20" id="html" role="tabpanel"><%= form.text_area :description, placeholder: "Texto", required: true, class: "form-control", id: :post_description %></div>
    </div>
  </div>
</div>

<div class="form-group row justify-content-center">
  <div class="col-8">
  <%= form.button "Enviar", class: "btn btn-success waves-effect waves-light", data: { "disable-with": "<i class='fa fa-spinner fa-spin'></i> Postando..." } %>
  </div>
</div>

  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<% end %>
